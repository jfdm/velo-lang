Running Test
Velo> # Finished Parsing
# Finished Type-Checking
Velo> let m = zero
in let n = (inc zero)
   in (add (add (add m n) (add n m)) (add (add n m) (add m n)))
Velo> let m = zero
in let n = (inc zero)
   in let p = (add n m)
      in let q = (add m n)
         in (add (add q p) (add p q))
Velo> Quitting, Goodbye.
